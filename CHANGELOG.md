# Changelog — CmdSpace Pilot

> upstream [joonlab/MacPilot](https://github.com/joonlab/MacPilot)의 브랜디드 포크.
> 여기에는 포크 이후의 변경만 기록합니다.

## v0.5.1 — 2026-08-23 (태블릿 가로: 우측 세로 레일 + 본문 다중 컬럼)

### 🔒 공개 배포 정리
- 개인 볼트명·기기명·운영 주소·사용 통계를 공개 기본값과 문서에서 제거
- TLS와 선택적 로컬 통합 토큰을 macOS Keychain으로 이동하고 인증서 파일을 Git에서 제외
- WebSocket same-origin·로컬 Host 검증, 민감한 세션 통합의 PIN 요구, 기본 보안 응답 헤더 추가
- 세션 화면·초안·검색 기록 캐시는 장기 저장 대신 현재 브라우저 세션에만 보존
- 이전 버전의 하드코딩 볼트 바로가기는 자동 이전하지 않음 — 업데이트 후 웹 설정에서 기기별 볼트명 재입력
- 앱 버전을 `project.yml`과 `Info.plist`에서 0.5.1로 단일화

### 🎛 가로 모드에서 퀵바를 우측 세로 레일로
- **버그**: 소프트 키보드가 뜨면 `html.kb-open`이 퀵바를 통째로 숨겨서 —
  타이핑하다 받아쓰기로 바꾸려는 **바로 그 순간** — 음성 지시·받아쓰기 버튼이 사라졌다.
- 태블릿 가로(`html.wide.orient-land`)에서 `#quickwrap`을 우측 고정 세로 레일(84px)로 이동.
  `bottom: var(--kb-height)`라 키보드가 떠도 그 위에 남고, 넘치면 레일이 스크롤된다.
  음성 지시·받아쓰기가 항상 최상단 — 키보드를 대체하는 수단이라 같이 숨으면 안 된다.
- **조건은 `docked`가 아니라 `wide`**: `docked`는 `min(vw,vh) ≥ 680`을 요구하는데
  Safari 주소창·툴바를 뺀 iPad mini 가로는 654px라 도킹 판정이 안 떨어진다(실측).
  레일이 필요한 건 '수직이 짧은 가로'이지 '멀티패널이 켜진 상태'가 아니므로 분리.
- `position:fixed` 오버레이(세션 시트·PTT 배너·음성 필)도 레일 폭만큼 비켜준다 —
  body 패딩은 fixed 요소에 닿지 않는다. 세션 시트는 프롬프트를 타이핑하는 곳이라 특히 중요.
- 옛 '가로 퀵바 2줄 좌우 배치' 규칙은 제거(레일로 대체) — 공존하면 파일 순서에 의존.
- 하단 69px을 회수해 본문 수직 공간 확보. 세로·폰은 기존 하단 퀵바 그대로.
- 미러 전체화면에서는 레일도 함께 접힘.

### 🧱 본문 다중 컬럼 (1 → 2 → 3열)
- `#sess-cards`(결정 대기)·`#srch-results`(검색)를 패널 실폭 기준 다중 컬럼으로.
  분할 슬라이더로 패널 폭이 연속 변하므로 **컨테이너 쿼리** 사용(미디어 쿼리로는 실폭을 모름).
- 임계값 실측 근거: iPad mini 가로 1133 → 레일 제외 후 기본 분할 586px = 2열(293px/칸),
  컴패니언 '없음' 1025px = 3열(336px/칸). 미지원 브라우저는 1열로 안전하게 남는다.
- `#cmux-remote`는 헤더·칩·행이 섞인 이질 구조라 제외(2열로 만들면 깨짐).

## v0.5.0 — 2026-08-21 (OmniControl 음성·세션 스위트)

### 🎙 OmniControl 음성 통합
- 퀵바 superwhisper(⌥Space) → **음성 지시**(⌃⌥⌘Space)·**받아쓰기**(⌃⌥⌘B) — 탭=토글
- **음성 상태 필**: 맥에서 어떤 레인이 녹음/인식 중인지 상단 필 + 버튼 적색 펄스 (`/state.voice`)
- **PTT 전송 확인 배너**: 맥 확인 카드를 폰에서 세션 선택/전송/취소 — 배너가 뜨면
  hold로 맥 카운트다운 자동 정지 (`/state.ptt` + omniPtt 프록시)

### 🔎 검색 탭 (세션 코퍼스 모바일판)
- 전 Claude Code 세션 풀텍스트 검색 + 작업폴더→세션 브라우즈, «하이라이트» 스니펫,
  최근 질의 칩 — 히트 탭 = 맥에서 열기/닫힌 세션 부활 (`GET /search`·`POST /focus` 프록시)

### 🃏 결정 카드 고도화
- cmux 워크스페이스 **색 매칭**(좌측 바+색점) + 그룹 소속 + **사이드바 필 코멘터리**
  (결정 힌트·사분면·Running을 cmux 색 그대로 — `statuses` 동사, 카드 ws만 지연 조회)
- **원클릭 답장 칩**: OmniControl 요약기의 decisions를 ↩ 칩으로 — 탭 한 번에 그 세션으로 전송
- 세션 시트 **뒤로가기 스와이프**(왼쪽 가장자리)

### 📐 레이아웃/탭 재편
- 탭 6→6 재편: **에이전트 · 검색 · 터미널 · 키보드 · 미러 · 덱** (herdr 전용 탭 제거 —
  상태는 cmux 트리 점·화면은 터미널 스위치로 수렴)
- iPad: 도킹 바의 중복 패널 배치 세그 제거(mini 세로 잘림 해소), 가로 도킹 시 퀵바 2줄→1줄
- **빠른 지시**: 일반적인 에이전트 작업 흐름을 바탕으로 한 편집 가능한 기본 문구
  (진행해·현재 상태는?·응 해줘·커밋하고 푸시해줘·…) + ✎ 편집(localStorage)

## v0.4.0 — 2026-07-03

### 🤖 에이전트 원격 (cmux · Claude Code) — 포크 시그니처 기능
- **에이전트 탭** 신설: 침대에서 코딩 에이전트를 승인/조종하는 모드
  - 큰 제어 버튼: ⏎ 입력·승인 / esc 중단 / ⌃C 강제 중단, ↑↓⇥·y⏎·n⏎
  - 빠른 지시(누르면 타이핑+엔터까지 자동): 계속해줘 · 진행상황 요약 · /compact · /clear · 테스트 실행 · 커밋해줘
- **cmux 브리지** (`CmuxBridge.swift`): cmux RPC 직접 호출로 **창·워크스페이스·탭 전환**
  - 워크스페이스가 이름+색상 칩으로 표시, 탭 제목으로 에이전트 상태 확인
  - 화이트리스트 4개 동사만 허용, UUID 검증, 셸 미경유 — 무인증 LAN 서버에 안전하게 통합
  - 소켓 인증: cmux `automation.socketPassword`를 로컬에서만 읽음 (폰에 미노출)
  - 에이전트 탭 표시 중 4초 자동 갱신 (변경 없으면 리렌더 생략)

### 🖱 커서 반응성/부드러움
- **TCP_NODELAY + `.responsiveData`**: Nagle이 move 프레임을 뭉치던 "낮은 주사율 느낌"의 근본 원인 제거
- **모션 전송 큐**: 프레임 단위 델타 병합, 리딩엣지 즉시 전송(rAF 대기 지연 제거)
- **RTT 자동 네트워크 프리셋(기본값)**: 3초 핑 측정으로 전송 주사율 36–120Hz 자동 조정
  - 수동 프리셋(빠른 Wi-Fi 120Hz/균형/불안정) + 주사율·보정·해상도 배율 슬라이더
- `EventInjector.move` 위치 조회 1회로 축소

### 📱 접속/레이아웃
- **PWA**: manifest + 홈 화면 아이콘 → 원탭 전체화면(standalone) 실행
- **가시 높이 추적(`--app-height`)**: 사파리 주소창 접힘/웹앱 모드의 하단 빈 공간 해결
- **트랙패드 시트 디텐트**: 풀/45%/70%/닫힘 스냅 + 높이 기억 → 덱과 레이어 분할 사용
- **화면 모드**: 자동(폭 640px)/폰/태블릿 — 와이드에서 덱 4열·12버튼
- **Tailscale 원격**: 테일넷 기기라면 어느 네트워크에서든 `…ts.net:8766` 영구 주소로 접속 (코드 변경 불필요, Funnel 금지)
- 안드로이드: IP 접속 가이드 + 햅틱(진동) 피드백

### 🎨 CMDSPACE 브랜딩/UI
- CMDS 라운드 로고(파비콘·터치 아이콘·앱 아이콘 .icns·메뉴 헤더), 브랜드 컬러(다크 Pink `#E985A2` / 라이트 Green `#134538`)
- UI 크롬 아이콘 전부 SF Symbols 풍 인라인 SVG (탭바·퀵바·미디어바·설정)
- **퀵바** 신설(상시 노출): superwhisper ⌥Space · Raycast ⇧Space · 앱 전환 · 미션 컨트롤 · 캡처
- 메뉴바 아이콘 상태 표시: 권한 없음 ⚠️ / 서버 다운 / 정상 📡
- 네이티브 메뉴 UI: 상태 타일·QR·권한 패널·PIN 페어링·서버 제어 (SwiftUI + SF Symbols)

### 🛠 운영/개발 루프
- **LaunchAgent 상시 서버**: 로그인 자동 시작 + 자동 재시작, `deploy.sh`가 plist 자동 생성
- `deploy.sh`: Apple 인증서 없으면 ad-hoc 폴백(경고 출력), 포트 자동 감지
- `script/macpilotctl.sh`: status/start/stop/restart/logs/open/url/install/**sync-web**/unsync-web
- **웹 오버라이드 서빙**: `sync-web`으로 웹 수정을 재빌드(=ad-hoc 권한 리셋) 없이 즉시 반영
- 기본 포트 8766

### ⬆️ upstream 동기화 (2026-07-03 병합)
- 다중 클라이언트 서버 강화: 전용 큐·자산 메모리 캐시·유휴 타임아웃·연결 상한·FD 상향 (박준님, 30대 동시 접속 현장 검증)
- **PIN 페어링**(기본 off): 공용 Wi-Fi에서 6자리 PIN 1회 입력 요구
- Windows 포트(커뮤니티) 문서/코드

### ↩️ upstream에 보낸 PR
- [joonlab#2](https://github.com/joonlab/MacPilot/pull/2) TCP_NODELAY · [joonlab#3](https://github.com/joonlab/MacPilot/pull/3) 웹 오버라이드 서빙 · [joonlab#4](https://github.com/joonlab/MacPilot/pull/4) PWA

## v0.2.0 — 포크 시점 (upstream 기준선)
트랙패드(모멘텀 스크롤·핀치·3손가락 제스처)·유니코드 키보드·스트림덱식 덱(단축키/텍스트/앱/매크로)·덱 서버 동기화·메뉴바 QR — 원작 [Park Joon (JoonLab)](https://github.com/joonlab/MacPilot).
