# CmdSpace Pilot — 로드맵 & 백로그

> 2026-07-07 기준. 이 문서는 코드 감사(에어마우스·iPad 분할·전반 TODO 병렬 감사)와 개발 이력을 종합한
> "못 끝낸 것 · 알려진 한계 · 앞으로 추가할 것" 정리본이다. CmdMD로 열어 읽기 좋게 작성.
>
> **상태 범례** — 🔴 안 됨/막힘 · 🟡 부분 구현 · 🟢 동작하나 개선 여지 · ⚫ 죽은 코드(미연결) · 💡 아이디어
> **난이도** — S(반나절) · M(1~2일) · L(며칠+) · XL(아키텍처 대공사)

---

## 이번 세션 완료 로그 (2026-07-07)

- **에어마우스 살림** 🟢 — iOS 모션 센서 HTTPS 요건 해결. `tailscale serve`로 확인 후, **앱 :443 + LE 인증서
  (예: `control.example.com`) + private DNS** 조합으로 확정. 권한 요청을 `pointerdown`→`click`으로 분리(iOS 팝업),
  안내를 iOS 17+ 실제 모델로 정정. 커밋 `24de9aa`, `d825d0e`.
- **커스텀 도메인 무포트 HTTPS** 🟢 — private network 전용 + 유효 인증서 + wss. 메뉴바에 주소 광고.
- **에어마우스 방향 90° 교정** 🟢 — `rotationRate.alpha`(롤)→`gamma`(yaw) + 화면방향 보정.
- **미러 핀치줌/팬 + 3·4핑거 제스처(트랙패드 공유)** 🟢 — 워크플로우 설계+적대검증. 커밋 `38958bc`.
- **3·4핑거 오발화 수정** 🟢 — 손가락 추가마다 기준 재설정 + 정착창. 커밋 `1d134ea`.
- **제스처 매핑 macOS 표준화 + 감도** — 4핑거 공간/미션/엑스포제, 3핑거 탐색/탭. 커밋 `b7e8ae4`.
- **한글 IME(터미널·미러)** 🟢 — 진짜 input으로만 조합. **도킹 컴패니언 슬롯** 🟢. 커밋 `00865c6`.
- **로드맵 문서** — `ROADMAP.md` + `docs/roadmap.html`(로컬 뷰). 코드 감사 23건 종합.

---

## 요약: 지금 상태 한눈에

| 영역 | 상태 | 한 줄 |
|---|---|---|
| 트랙패드/키보드/덱/클릭/스크롤 | 🟢 | 핵심 기능 안정 |
| 마우스 부드러움(1€+보간) | 🟢 | 완료 |
| 한글 IME 입력(터미널·미러) | 🟢 | Termius식으로 해결 |
| 미러(화면 보기)+핀치줌·팬 | 🟢 | MJPEG 경량, 오디오·코덱 없음 |
| cmux 터미널 뷰 | 🟢 | 동작 |
| 3·4핑거 제스처 인식 | 🟡 | 인식은 됨, 사용성 보통 |
| **에어마우스(모션)** | 🟢 | **해결 — private HTTPS에서 동작 + 방향축(gamma) 교정** |
| **커스텀 HTTPS** | 🟢 | **앱 :443 + 유효 인증서 · private DNS · 무포트 HTTPS** |
| **데스크탑(공간) 전환** | 🔴 | **macOS 26이 합성 전환 차단** |
| iPad 도킹 분할 | 🟡 | 코어는 OK, 스플리터 오프셋·터치타깃 버그 |
| 화면기록 권한 안내 | 🟡 | 인앱 UI 없음(손쉬운사용만 폴링) |
| 발표 스팟라이트 | 🟡 | 반경 조절·전용 UI·off() 미완 |

---

## 1. 지금 안 되는 것 (우선 수정)

### 1.1 🟢 에어마우스(모션 센서) — 해결됨 (2026-07-07)
> **완료**: private HTTPS 주소에서 동작 확인. 아래는 해결 경위(기록용).
> - HTTPS: 앱 `:443` 리스너가 유효 인증서로 서빙. DNS는 private network 안에서만 Mac을 가리킨다.
> - 권한: iOS 17+는 설정 메뉴 없이 '팝업'만 → 요청을 `pointerdown`→`click`(iOS가 인정하는 제스처)으로 분리.
> - 방향: `onAirMotion`이 `alpha`(롤) 대신 `gamma`(yaw)를 수평에 쓰도록 교정(90° 어긋남 해소) + 화면방향 보정.

<details><summary>원래 진단(참고)</summary>

#### (구) 에어마우스(모션 센서) — HTTPS 보안 컨텍스트 필요 · 난이도 M
- **증상**: 에어 버튼을 누르면 "http에선 모션 센서 차단" 안내가 뜨고 동작 안 함.
- **근본 원인**: iOS는 `DeviceMotion/DeviceOrientation`을 **secure context(HTTPS/localhost)에서만** 허용.
  현재 서버는 `http://<name>.local:8766` / `http://…ts.net:8766`(Tailscale)만 광고 → `window.isSecureContext=false` →
  `airStart()`가 즉시 차단(app.js). **모션→커서 코드(rotationRate + orientation 폴백 + 1€ 필터)와 권한 요청
  흐름(requestPermission)은 이미 올바르게 구현돼 있고, 오직 HTTP 오리진 때문에 도달을 못 할 뿐이다.**
- **이미 있는 것**: `HelperServer`에 조건부 `:443` TLS 리스너(`listener443`)가 존재
  (`App Support/CmdPilot/tls/pilot.p12`, 암호는 macOS Keychain, CN은 배포별 도메인). 하지만 `updateURL()`이
  **무조건 http URL만** 만들어 메뉴/QR에 노출 → 인증서를 깔아도 http QR이 나와 에어마우스가 죽음.
- **해결안(택1)**:
  1. **(권장) `tailscale serve`** 로 로컬 서버 앞단에 Tailscale 발급 TLS를 붙여 `https://<mac>.<tailnet>.ts.net`
     제공 → 유효 secure context + wss 자동 승격(app.js는 https면 wss 선택). 그 https URL을 메뉴/QR에 광고.
     인증서·DNS 부담 없음.
  2. 기존 `:443` p12 경로 완성 — `listener443 != nil`이면 인증서 CN의 HTTPS 주소를 광고
     (`.local`은 LE 인증서 CN과 불일치하니 도메인 사용). DNS가 폰에서 맥으로 해석돼야 함.
  3. 스톱갭: `<name>.local`용 자체서명/mkcert 인증서 + 폰에 신뢰 설치 안내.
- **부수 작업**: 안내문을 "보안 컨텍스트(HTTPS)로 접속" 우선으로 재정렬(iOS 26/27은 사이트별 권한 거부가 아니라
  secure context 부재가 진짜 원인). 메뉴/QR에 "에어마우스용 https" 라벨.
- **파일**: `HelperServer.swift`(updateURL/loadTLSOptions), `MenuContentView.swift`, `app.js`(airStart 가드/안내).

</details>

### 1.2 🔴 데스크탑(공간) 좌/우 전환 — macOS 26에서 차단 · 난이도 L(우회 연구)
- **증상**: 4핑거 좌/우(데스크탑 전환) 기본 배정이 실제로는 무동작.
- **근본 원인**: `EventInjector.gesture()` 주석에 명시 — **macOS 26이 공간 전환을 합성 CGEvent(⌃←/→)와 CGS/SkyLight
  비공개 API 양쪽 모두 차단**. 즉 키 주입·비공개 API 어느 쪽으로도 프로그램적 공간 전환 불가.
- **영향**: 방금 넣은 `s4left/s4right = spaceLeft/spaceRight`가 이 맥에선 동작 안 할 수 있음.
- **선택지**:
  - 4핑거 좌/우를 macOS 26에서 실제 되는 액션으로 재배정(앱 전환/뒤로·앞으로/미션컨트롤 조합 등),
  - 또는 spaceLeft/spaceRight에 "이 macOS에서 미지원" 경고 노출,
  - 또는 미션컨트롤(⌃↑, 동작함)에서 공간을 눈으로 고르는 방식 안내.
- **참고**: 미션컨트롤 `⌃↑`·엑스포제 `⌃↓`는 정상 동작(네이티브 애니메이션 재생).

### 1.3 🟡 iPad 도킹 분할 버그 · 난이도 S~M
- **스플리터 드래그 오프셋**: 도킹 `<main>`의 padding(8/12px+safe-area)과 14px 스플리터 트랙을 빼지 않고
  정규화 → 드래그가 손가락과 어긋남. 그리드 내부 시작/크기를 빼고 계산 필요.
- **기본 dockSplit 0.42가 어떤 프리셋과도 불일치** → 작게/균형/크게 3버튼이 처음에 모두 미선택으로 보임.
  기본을 프리셋값(0.38 또는 0.5)으로 맞추거나 하이라이트 허용오차 확대/"커스텀" 표기.
- **스플리터 터치 타깃 14px** → 44px 가이드라인 미달, 태블릿에서 잡기 어려움. hit 영역 20~24px로 확대(::before).
- **컴패니언=키보드 + 키보드 탭 동시**: 레이아웃이 전체 키보드로 붕괴되고 크기 프리셋이 상태와 어긋남 →
  effective 상태를 UI에 반영(강등 시 '없음' 하이라이트 등).
- **코어(fr 계산·방향·클램프 0.3~0.72·회전·localStorage)는 버그 없음**으로 검증됨.
- **파일**: `app.js`(applyDockSplit/스플리터 드래그/setDockCompanion), `style.css`(도킹 그리드).

### 1.4 🟡 화면기록 권한 인앱 안내 없음 · 난이도 S
- 미러/캡처는 손쉬운사용과 **별개인 '화면 및 시스템 오디오 기록' 권한**이 필요한데, 메뉴바 UI는 손쉬운사용만
  폴링·안내. 권한 없으면 미러가 조용히 안 뜸.
- **필요**: `MenuContentView`에 화면기록 권한 타일 + `설정 열기`(`x-apple.systempreferences:...ScreenCapture`),
  최초 사용 전 프리플라이트 안내.

### 1.5 🟡 발표 스팟라이트 미완 · 난이도 S
- `off()`가 어디서도 호출 안 됨(죽은 코드) → 연결 종료 시 자동 off 연결 권장.
- 반경(radius=110 고정)을 폰에서 조절할 명령 경로 없음.
- 발표 모드 전용 버튼/UI 없음(제스처·덱 배정으로만 토글).

---

## 2. 알려진 한계 (설계상/현재 불가)

- **트랙패드 연속 제스처(손가락 추종·되감기)** ⚫ — 시스템 트랙패드 드라이버가 멀티터치 제스처 위상(phase)을
  넘겨야 하는데, `CGEvent` 키 주입으론 그 연속 이벤트를 만들 수 없음(비공개 API 영역). → 개별 단축키 발화가 최선.
- **미러 오디오·비디오 코덱** 🟡 — 현재 MJPEG intra-only(매 프레임 전체 JPEG). 오디오 없음, 동적 화면에서 대역폭↑.
  고품질은 VideoToolbox H.264 + WebCodecs/WebRTC 필요(XL). 지금은 "보며 조작"용 경량 뷰어로 의도됨.
- **공간 전환 프로그램 제어** 🔴 — 위 1.2. macOS 26 정책.

---

## 3. 정리 대상 (죽은 코드 — 미연결)

- ⚫ `SpaceSwitcher.swift` — 비공개 CGS/SkyLight 공간 전환. 호출부 0건. 심사·안정성 리스크 → 삭제 권장.
- ⚫ `EventInjector.gesture()` + `case "gesture"` — 웹이 `t:"gesture"`를 안 보냄(제스처는 raw key로 전송). 프로토콜 단순화 위해 제거 검토.
- ⚫ `SpotlightOverlay.off()` — 미사용. 연결하거나 제거.

---

## 4. 개선하면 좋은 것 (동작하지만)

- **3·4핑거 제스처 사용성** 🟡 — 인식은 되나 감(감도/오발화)이 완벽하진 않음. (재기준+정착창+우세축으로 개선했으나
  실기기 튜닝 여지). "손가락 뗄 때 발화(중간 되돌리면 취소)" 옵션으로 취소 가능성 부여 검토.
- **cmux 브리지 결합도** 🟢 — 소켓 인증 self-heal이 cmux 내부 JSON 스키마/에러 문자열에 강결합 → cmux 업그레이드에 취약.
  cmux 공식 automation-token API 나오면 이관. 1.8초 동기 sleep이 브리지 큐 점유.
- **Windows 포트 기능 매트릭스** 🟡 — Windows 서버엔 코어 입력만. mirror/capture/ocr/cmux/cterm/window/spotlight 미구현이나
  공유 웹 클라가 탭을 띄움 → 미지원 탭 숨김/표기 + 문서에 macOS/Windows 매트릭스 필요. 제스처 키코드 변환 검증.
- **로그 로테이션** 🟢 — `/tmp` 진단 로그가 무제한 append. 로테이션/비활성 옵션.
- **카메라 OCR** 🟢 — 실시간 카메라 아니라 파일 피커 1장. 빈 결과 무피드백 → 명시 메시지, (향후) 연속 스캔 모드.
- **활성 포트 표기** 🟢 — :80/:443이 실패해도 조용히 스킵, 메뉴엔 8766만. 실제 활성 포트 표기.
- **p12 암호 외부화** 🟢 — 소스에서 제거하고 macOS Keychain 조회로 전환.

---

## 5. 추가 기능 아이디어 (백로그)

### 입력·제어
- 💡 **제스처 취소 가능(fire-on-release)** — 중간에 방향 되돌리면 미발화 → 매직 트랙패드 "되돌림" 근사. 난이도 S.
- 💡 **커스텀 제스처를 매크로/앱실행에 배정** — 이미 GESTURE_ACTIONS 확장 가능. 슬롯에 매크로 연결. 난이도 S.
- 💡 **힘/속도 기반 스크롤 관성 튜닝 UI** — scrollSpeed/accel 프리셋. 난이도 S.
- 💡 **에어마우스 클릭 제스처**(폰 흔들기/탭) — HTTPS 해결 후. 난이도 M.

### 미러·화면
- 💡 **미러에서 특정 창/영역만 스트림**(SCContentFilter로 창 선택) — 프라이버시·대역 절감. 난이도 M.
- 💡 **미러 화질 프리셋 토글**(선명/절전) — 이미 티어 있음, 수동 토글 노출. 난이도 S.
- 💡 **H.264 미러**(VideoToolbox+WebCodecs) — 부드러운 영상. 난이도 XL.
- 💡 **미러 녹화/스냅샷 저장**. 난이도 M.

### 연결·온보딩
- 💡 **`tailscale serve` 자동 설정 스크립트 + 메뉴 토글** — 에어마우스/HTTPS 한 방에 해결. 난이도 M. (1.1과 연계)
- 💡 **QR에 https/http 둘 다 + "에어마우스용" 라벨**. 난이도 S.
- 💡 **첫 실행 온보딩**(권한 3종·URL·QR 가이드) — 손쉬운사용+화면기록+모션. 난이도 M.
- 💡 **연결 상태 히스토리/자동 재연결 견고화**. 난이도 S.

### 덱·워크플로우
- 💡 **덱 버튼에 이미지/앱 아이콘 자동**(AppList 확장). 난이도 S.
- 💡 **cmux 딥 통합**(터미널 뷰에서 세션 생성/분할/붙여넣기 — mobile.terminal.create/paste). 난이도 M.
- 💡 **클립보드 양방향**(맥↔폰 텍스트/이미지). 난이도 M.
- 💡 **덱 폴더 클라우드 동기/프리셋 공유**. 난이도 M.

### 플랫폼
- 💡 **Windows 시그니처 기능 포팅**(mirror/capture 우선). 난이도 L.
- 💡 **다중 맥 전환**(한 폰에서 여러 서버). 난이도 M.

---

## 부록: 우선순위 제안

**바로(고임팩트·저난이도)**
1. iPad 분할 스플리터 오프셋·터치타깃·기본값 수정 (1.3) — S
2. 화면기록 권한 인앱 안내 (1.4) — S
3. 4핑거 좌/우(공간전환) macOS 26 미지원 처리/재배정 (1.2 부분) — S
4. 죽은 코드 정리 (3) — S

**다음(핵심 가치)**
5. **에어마우스 HTTPS 해결** — `tailscale serve` 경로 (1.1) — M ★ 사용성 가치 높은 기능
6. 발표 스팟라이트 완성 (1.5) — S
7. 제스처 취소(fire-on-release) 옵션 — S

**중장기**
8. Windows 시그니처 기능 포팅 — L
9. H.264 미러 — XL
10. 온보딩·클립보드 양방향 — M

---

*이 문서는 세션별로 갱신. 완료 항목은 상태를 🟢로 바꾸고 커밋 로그와 함께 남긴다.*
